#include <stdlib.h>
#include <stdio.h>
#include "BSTree.h"

int BSTreeNumNodes(BSTree t) { return 0; }

int BSTreeHeight(BSTree t) { return 0; }

int BSTreeNodeLevel(BSTree t, int key) { return 0; }

int BSTreeCountGreater(BSTree t, int key) { return 0; }


int main(int argc, char *argv[]) {
    BSTree t = readBSTree(0);
}